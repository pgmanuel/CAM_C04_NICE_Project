# Quick Start

```bash
cd CAM_Project/src/justification

python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt

cp .env.example .env
# fill in LLM_MODEL and your API key in .env

python demo.py
```
